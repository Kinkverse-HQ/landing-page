# Instruction provenance

This repository had no tracked root AGENTS, Claude or Gemini instruction file at the selected base. Added adapters all point to `AGENTS.md`; scoped contracts record verified project boundaries.
The owner's branch, attribution, automatic checkpoint/push, draft-review and human merge requirements are in workflow/PR contracts. Screenshot inspection and provenance requirements are retained for UI work.
No product implementation or existing local user artifact is changed by these instructions.
